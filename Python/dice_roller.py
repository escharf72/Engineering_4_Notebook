# Automatic Dice Roller
# Written by Elisabeth

	
from random import randint
#value = input("Press enter to roll") 

while (True): 
	value = input("Press enter to roll")
	if input(""):
		#print ("yay")
		print (randint(1,6)) 
		#value = input("Press x and then enter to exit")
	if input("x"):
		exit() 
 
